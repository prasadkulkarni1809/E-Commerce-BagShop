<!-- NAVBAR -->
<nav class="bg-white dark:bg-gray-900 border-b dark:border-gray-700 px-8 py-4 flex justify-between items-center">

  <!-- LEFT : LOGO -->
  <h1 class="text-2xl font-extrabold tracking-tight text-gray-900 dark:text-white">
    Bag<span class="text-gray-400">.</span>
  </h1>

  <!-- RIGHT : NAV LINKS -->
  <div class="flex items-center gap-6 text-sm font-medium text-gray-600 dark:text-gray-300">

    <!-- SHOP -->
    <a href="/shop" class="flex items-center gap-2 hover:text-black dark:hover:text-white transition">
      🛍️ Shop
    </a>

    <!-- MY ACCOUNT -->
    <a href="/users/account" class="flex items-center gap-2 hover:text-black dark:hover:text-white transition">
      👤 My Account
    </a>

    <!-- CART -->
    <a href="/cart" class="relative flex items-center gap-2 hover:text-black dark:hover:text-white transition">
      🛒 Cart

      <% if (cartCount > 0) { %>
        <span
          class="absolute -top-2 -right-3 bg-black dark:bg-white text-white dark:text-black text-[10px] w-5 h-5 flex items-center justify-center rounded-full">
          <%= cartCount %>
        </span>
      <% } %>
    </a>

    <!-- DARK MODE TOGGLE -->
    <button
      id="darkToggle"
      class="ml-2 w-9 h-9 flex items-center justify-center rounded-full border border-gray-300 dark:border-gray-600
             hover:bg-gray-100 dark:hover:bg-gray-700 transition"
      aria-label="Toggle Dark Mode">

      <span id="darkIcon">🌙</span>
    </button>

    <!-- DIVIDER -->
    <span class="h-4 w-px bg-gray-300 dark:bg-gray-600"></span>

    <!-- LOGOUT -->
   <% if (showLogout) { %>
  <span class="h-4 w-px bg-gray-300"></span>

  <a href="/users/logout"
     class="flex items-center gap-2 text-red-500 hover:text-red-600 transition">
    🚪 Logout
  </a>
<% } %>

  </div>
</nav>
